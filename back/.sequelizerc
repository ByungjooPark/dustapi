const path = require('path');

module.exports = {
	// config/database.json 파일을 Sequelize 설정으로 사용
  config: path.resolve('configs', 'sequelizeCliConfig.js'),
  // 모델 파일을 db/models 폴더에서 관리
  'models-path': path.resolve('models'),
  // 시더 파일을 db/seeders 폴더에서 관리
  'seeders-path': path.resolve('seeders'),
  // 마이그레이션 파일을 db/migrations 폴더에서 관리
  'migrations-path': path.resolve('migrations'),
};